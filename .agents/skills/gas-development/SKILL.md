---
name: gas-development
description: GAS開発やエンドポイント設定の専門知識
---

# Skill: GAS Development

## GAS Endpoint Configuration
- フロントエンドは `client_core.js` や `config.js` を通じてGASのURLを解決する。
- GAS側は `doPost` および `doGet` でルーティングを行う。
- マルチテナント設定はテナントごとの設定ファイルで分離する。
